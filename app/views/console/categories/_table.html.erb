<table class="table">
<thead>
<tr>
<td>代码</td>
<td>类型名称</td>
<td>服务数量</td>
<td>操作</td>
</tr>
</thead>
<tbody>
<% @categories.each do |cate| %>
<tr data-id="<%= cate.id %>">
<td><%= cate.code %></td>
<td><%= cate.name %> <%= cate.en_name %></td>
<td><%= cate.services_count %></td>
<td>

<div class="btn-group btn-group-sm">
  <button type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown">
    操 作 <span class="caret"></span>
  </button>
  <ul class="dropdown-menu" role="menu">
   <li><%= link_to raw('<i class="glyphicon glyphicon-edit"></i> 修改'),edit_console_category_path(cate) %></li>
<li>
<%= link_to raw('<i class="glyphicon glyphicon-trash"></i> 删除'),console_category_path(cate),:method=>:delete,:format=>"js",:remote=>true,:confirm=>"Are you sure?" %></li>
  </ul>
</div>
</td>
</tr>
<%end%>
</tbody>
</table>
